#!/bin/bash
# Custom run: set SERVER_PORT, then delegate to the builder image's
# original run script.
export SERVER_PORT=8081
echo "---> distributor-backend custom run (SERVER_PORT=${SERVER_PORT})"
exec /usr/libexec/s2i/run
